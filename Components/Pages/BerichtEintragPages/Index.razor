@page "/BerichtEintraeges"
@using Microsoft.EntityFrameworkCore
@using Microsoft.AspNetCore.Components.QuickGrid
@using xAzubiLog.Models
@using xAzubiLog.Data
@implements IAsyncDisposable
@inject IDbContextFactory<xAzubiLog.Data.xAzubiLogContext> DbFactory

<PageTitle>Index</PageTitle>

<h1>Index</h1>

<p>
    <a href="BerichtEintraeges/create">Create New</a>
</p>

<QuickGrid Class="table" Items="context.BerichtEintraege">
    <PropertyColumn Property="entry => entry.BenutzerId" />
    <PropertyColumn Property="entry => entry.AusbilderId" />
    <PropertyColumn Property="entry => entry.KategorieId" />
    <PropertyColumn Property="entry => entry.WochenberichtId" />
    <PropertyColumn Property="entry => entry.Datum" />
    <PropertyColumn Property="entry => entry.Tagestyp" />
    <PropertyColumn Property="entry => entry.Auftragsnummer" />
    <PropertyColumn Property="entry => entry.Titel" />
    <PropertyColumn Property="entry => entry.Beschreibung" />
    <PropertyColumn Property="entry => entry.Notiz" />
    <PropertyColumn Property="entry => entry.Fach" />
    <PropertyColumn Property="entry => entry.Startzeit" />
    <PropertyColumn Property="entry => entry.Endzeit" />
    <PropertyColumn Property="entry => entry.Dauer" />
    <PropertyColumn Property="entry => entry.Status" />
    <PropertyColumn Property="entry => entry.ErstelltAm" />
    <PropertyColumn Property="entry => entry.GeändertAm" />

    <TemplateColumn Context="entry">
        <a href="@($"BerichtEintraeges/edit?id={entry.Id}")">Edit</a> |
        <a href="@($"BerichtEintraeges/details?id={entry.Id}")">Details</a> |
        <a href="@($"BerichtEintraeges/delete?id={entry.Id}")">Delete</a>
    </TemplateColumn>
</QuickGrid>

@code {
    private xAzubiLog.Data.xAzubiLogContext context = default!;

    protected override void OnInitialized()
    {
        context = DbFactory.CreateDbContext();
    }

    public async ValueTask DisposeAsync() => await context.DisposeAsync();
}
