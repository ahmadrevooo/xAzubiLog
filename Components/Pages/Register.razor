@page "/register"
@rendermode InteractiveServer
@inject xAzubiLog.Data.xAzubiLogContext DB
@inject NavigationManager Nav

<PageTitle>Registrieren – xAzubiLog</PageTitle>

<div class="login-wrapper">
    <div class="card">
        <div class="card2">
            <form class="form">
                <p id="heading">xAzubiLog</p>

                @if (fehler != null)
                {
                    <div style="color:#ff6b6b; text-align:center; font-size:0.85rem; margin-top:-0.5rem;">@fehler</div>
                }
                @if (erfolg)
                {
                    <div style="color:#00ff75; text-align:center; font-size:0.85rem;">
                        Erfolgreich! <a href="/login" style="color:#a0b4cc;">Anmelden</a>
                    </div>
                }

                <div class="field">
                    <svg class="input-icon" viewBox="0 0 16 16" fill="currentColor" height="16" width="16" xmlns="http://www.w3.org/2000/svg">
                        <path d="M8 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm2-3a2 2 0 1 1-4 0 2 2 0 0 1 4 0zm4 8c0 1-1 1-1 1H3s-1 0-1-1 1-4 6-4 6 3 6 4z"/>
                    </svg>
                    <input @bind="vorname" autocomplete="off" placeholder="Vorname" class="input-field" type="text" />
                </div>

                <div class="field">
                    <svg class="input-icon" viewBox="0 0 16 16" fill="currentColor" height="16" width="16" xmlns="http://www.w3.org/2000/svg">
                        <path d="M8 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm2-3a2 2 0 1 1-4 0 2 2 0 0 1 4 0zm4 8c0 1-1 1-1 1H3s-1 0-1-1 1-4 6-4 6 3 6 4z"/>
                    </svg>
                    <input @bind="nachname" autocomplete="off" placeholder="Nachname" class="input-field" type="text" />
                </div>

                <div class="field">
                    <svg class="input-icon" viewBox="0 0 16 16" fill="currentColor" height="16" width="16" xmlns="http://www.w3.org/2000/svg">
                        <path d="M13.106 7.222c0-2.967-2.249-5.032-5.482-5.032-3.35 0-5.646 2.318-5.646 5.702 0 3.493 2.235 5.708 5.762 5.708.862 0 1.689-.123 2.304-.335v-.862c-.43.199-1.354.328-2.29.328-2.926 0-4.813-1.88-4.813-4.798 0-2.844 1.921-4.881 4.594-4.881 2.735 0 4.608 1.688 4.608 4.156 0 1.682-.554 2.769-1.416 2.769-.492 0-.772-.28-.772-.76V5.206H8.923v.834h-.11c-.266-.595-.881-.964-1.6-.964-1.4 0-2.378 1.162-2.378 2.823 0 1.737.957 2.906 2.379 2.906.8 0 1.415-.39 1.709-1.087h.11c.081.67.703 1.148 1.503 1.148 1.572 0 2.57-1.415 2.57-3.643zm-7.177.704c0-1.197.54-1.907 1.456-1.907.93 0 1.524.738 1.524 1.907S8.308 9.84 7.371 9.84c-.895 0-1.442-.725-1.442-1.914z"></path>
                    </svg>
                    <input @bind="email" autocomplete="off" placeholder="E-Mail" class="input-field" type="email" />
                </div>

                <div class="field">
                    <svg class="input-icon" viewBox="0 0 16 16" fill="currentColor" height="16" width="16" xmlns="http://www.w3.org/2000/svg">
                        <path d="M8 1a2 2 0 0 1 2 2v4H6V3a2 2 0 0 1 2-2zm3 6V3a3 3 0 0 0-6 0v4a2 2 0 0 0-2 2v5a2 2 0 0 0 2 2h6a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2z"></path>
                    </svg>
                    <input @bind="passwort" placeholder="Passwort" class="input-field" type="password" />
                </div>

                <div class="field">
                    <svg class="input-icon" viewBox="0 0 16 16" fill="currentColor" height="16" width="16" xmlns="http://www.w3.org/2000/svg">
                        <path d="M8 1a2 2 0 0 1 2 2v4H6V3a2 2 0 0 1 2-2zm3 6V3a3 3 0 0 0-6 0v4a2 2 0 0 0-2 2v5a2 2 0 0 0 2 2h6a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2z"></path>
                    </svg>
                    <input @bind="passwortBestaetigen" placeholder="Passwort bestaetigen" class="input-field" type="password" />
                </div>

                <div class="field">
                    <svg class="input-icon" viewBox="0 0 16 16" fill="currentColor" height="16" width="16" xmlns="http://www.w3.org/2000/svg">
                        <path d="M7 14s-1 0-1-1 1-4 5-4 5 3 5 4-1 1-1 1H7zm4-6a3 3 0 1 0 0-6 3 3 0 0 0 0 6z"/>
                    </svg>
                    <select class="input-field" style="color:#d3d3d3;" @bind="rolle">
                        <option value="Azubi" style="background:#171717;">Azubi</option>
                        <option value="Ausbilder" style="background:#171717;">Ausbilder</option>
                    </select>
                </div>

                <div class="btn">
                    <button class="button2" type="button" @onclick='() => Nav.NavigateTo("/login")'>Anmelden</button>
                    <button class="button1" type="button" @onclick="RegisterAsync">Registrieren</button>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    .login-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background: #171717;
    }

    .card {
        background-image: linear-gradient(163deg, #00ff75 0%, #3700ff 100%);
        border-radius: 22px;
        transition: all 0.3s;
    }

    .card2 {
        border-radius: 0;
        transition: all 0.2s;
    }

    .card2:hover {
        transform: scale(0.98);
        border-radius: 20px;
    }

    .card:hover {
        box-shadow: 0px 0px 30px 1px rgba(0, 255, 117, 0.3);
    }

    .form {
        display: flex;
        flex-direction: column;
        gap: 10px;
        padding-left: 2em;
        padding-right: 2em;
        padding-bottom: 0.4em;
        background-color: #171717;
        border-radius: 25px;
        transition: 0.4s ease-in-out;
    }

    #heading {
        text-align: center;
        margin: 2em;
        color: white;
        font-size: 1.2em;
    }

    .field {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5em;
        border-radius: 25px;
        padding: 0.6em;
        border: none;
        outline: none;
        color: white;
        background-color: #171717;
        box-shadow: inset 2px 5px 10px rgb(5, 5, 5);
    }

    .input-icon {
        height: 1.3em;
        width: 1.3em;
        fill: white;
    }

    .input-field {
        background: none;
        border: none;
        outline: none;
        width: 100%;
        color: #d3d3d3;
    }

    .input-field::placeholder {
        color: #6a85a0;
    }

    .form .btn {
        display: flex;
        justify-content: center;
        flex-direction: row;
        margin-top: 2.5em;
        margin-bottom: 1em;
        gap: 0.5em;
    }

    .button1 {
        padding: 0.5em 1.1em;
        border-radius: 5px;
        border: none;
        outline: none;
        transition: 0.4s ease-in-out;
        background-color: #252525;
        color: white;
        cursor: pointer;
    }

    .button1:hover { background-color: black; }

    .button2 {
        padding: 0.5em 2.3em;
        border-radius: 5px;
        border: none;
        outline: none;
        transition: 0.4s ease-in-out;
        background-color: #252525;
        color: white;
        cursor: pointer;
    }

    .button2:hover { background-color: black; }
</style>

@code {
    string vorname = "";
    string nachname = "";
    string email = "";
    string passwort = "";
    string passwortBestaetigen = "";
    string rolle = "Azubi";
    string? fehler;
    bool erfolg = false;

    async Task RegisterAsync()
    {
        fehler = null;

        if (string.IsNullOrWhiteSpace(vorname) || string.IsNullOrWhiteSpace(nachname) ||
            string.IsNullOrWhiteSpace(email) || string.IsNullOrWhiteSpace(passwort))
        {
            fehler = "Bitte alle Felder ausfuellen.";
            return;
        }

        var emailRegex = new System.Text.RegularExpressions.Regex(@"^[^@\s]+@[^@\s]+\.[^@\s]{2,}$");
        if (!emailRegex.IsMatch(email))
        {
            fehler = "Bitte eine gueltige E-Mail-Adresse eingeben.";
            return;
        }

        var nameRegex = new System.Text.RegularExpressions.Regex(@"^[^\d]+$");
        if (!nameRegex.IsMatch(vorname) || !nameRegex.IsMatch(nachname))
        {
            fehler = "Vor- und Nachname duerfen keine Zahlen enthalten.";
            return;
        }

        if (passwort != passwortBestaetigen)
        {
            fehler = "Passwoerter stimmen nicht ueberein.";
            return;
        }

        if (passwort.Length < 6)
        {
            fehler = "Passwort muss mindestens 6 Zeichen lang sein.";
            return;
        }

        bool emailExistiert = DB.User.Any(u => u.Email == email);
        if (emailExistiert)
        {
            fehler = "Diese E-Mail ist bereits registriert.";
            return;
        }

        var neuerUser = new xAzubiLog.Models.User
        {
            Vorname = vorname,
            Nachname = nachname,
            Email = email,
            PasswortHash = BCrypt.Net.BCrypt.HashPassword(passwort),
            Rolle = rolle,
            Aktiv = true
        };

        DB.User.Add(neuerUser);
        await DB.SaveChangesAsync();

        erfolg = true;
    }
}